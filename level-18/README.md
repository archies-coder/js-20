# Level 18 — Tooling: Linting, Formatting, CI/CD

Sub-levels

1. ESLint + Prettier configuration
2. Type-checking pipelines
3. CI: test, build, lint, bundle size

Acceptance criteria

- Repo-wide ESLint + Prettier + TypeScript setup
- CI config that runs tests, builds, and reports bundle size

Tasks

- `eslint-config`: minimal sharable config for this repo
- `prettier`: project config + scripts
- `ci`: GitHub Actions or local script to simulate CI steps
