# Level 20 — Capstone: Production-Grade App

Sub-levels

1. Requirements and scope definition
2. Architecture and tradeoffs
3. Delivery: reliability, security, performance

Acceptance criteria

- Build a small but production-grade app end-to-end
- Include tests, types, CI, docs, and deploy/run scripts

Tasks (choose one)

- `realtime-chat`: WebSocket server + React client + auth + rate limits
- `edge-image-service`: on-the-fly transforms with caching and quotas
- `analytics-pipeline`: event ingestion -> transform -> query interface
