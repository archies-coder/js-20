# Level 14 — Patterns & Architecture

Sub-levels

1. FP vs OOP tradeoffs in JS
2. Module boundaries and dependency injection
3. Evented/Reactive designs

Acceptance criteria

- Implement small composable modules with clear boundaries
- Explain why a chosen pattern fits the problem

Tasks

- `compose/pipe`: safe composition utilities with type inference via JSDoc
- `EventBus`: channels, unsubscribe, once, wildcard topics
- `Repository`: interface + in-memory impl with injection-friendly design
