# Level 17 — React Core & Ecosystem

Sub-levels

1. Reconciliation, keys, and rendering model
2. Hooks: rules, closures, memoization
3. State management patterns

Acceptance criteria

- Build React components without CRA; use Vite or ESBuild
- Demonstrate performance profiling and memoization wins

Tasks

- `hooks`: implement `useInterval`, `useEvent`, `useDebouncedValue`
- `state`: build a tiny store with context/selectors
- `suspense`: simulate data fetching and Suspense boundaries
